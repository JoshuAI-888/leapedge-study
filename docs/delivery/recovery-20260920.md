# Comparison recovery — 20 September 2026

Scope: repair the seven stopped cases and recheck four known output-quality failures from the immutable [20-case baseline](live-comparison-20-20260920.md). Reuse captured LeapEdge reports. No new LeapEdge analysis, no Finradar integration, and no human-verification labels.

## Changes

- Windowed audio requests explicitly require seconds, with per-window numeric bounds. Bad timing or truncated responses subdivide only the failed window down to a bounded minimum; successful windows remain checkpointed. The 65-minute podcast's retained failed response used milliseconds (1200000 instead of about 1200 seconds).
- Speech occupancy remains visible as measured. Re-listen to uncovered intervals over three seconds before treating a fully processed audio plan as ready for extraction. Partially filled gaps are checked recursively with a depth/call bound; an unresolved gap stops for review. Confirmed silence is recorded separately; inaccessible audio remains reviewable. This is model coverage, not proof of transcript accuracy.
- Extraction batches have both input and ten-minute duration bounds, low reasoning, and a 24k output allowance. Truncated batches split independently with new paid-stage identifiers and at most eight repairs. Source cues and conditions remain available across boundaries.
- Oversized evidence ranges split at real source cue boundaries into exact, hashed copies. Missing, reversed or otherwise unusable ranges reject that item with diagnostics rather than aborting unrelated claims.
- Explicit instructions retain options strikes/expiry, percentage targets and conditions, separate actionable calls from hypothetical scenarios, and split company groups only where the same source-backed instruction applies to each.
- Exact company aliases have sourced listing matches with the literal source ticker kept separate. An unsupported model ticker proposal is retained in diagnostics rather than attached to the claim; the independent critic must still validate its company thesis. Ambiguous groups, sectors and indices remain unresolved; matches do not confer audio/human trust.
- Flash Lite rates corrected to $0.25/M text input, $0.50/M audio input and $1.50/M output; historical calls are not rewritten. Source: [Google pricing](https://ai.google.dev/gemini-api/docs/pricing). Native amounts are token-based estimates; OpenRouter amounts use provider-reported usage cost.
- Explicitly linked completed recoveries clear historical failures from the attention panel. Original failed records remain accessible and are labelled Recovered in activity.
- Analysis chooses its initial player position from the same sorted claim shown as selected. Processing diagnostics include rejected pointers, ticker proposals and audio completeness. Accepted background/context points and their evidence are now visible, including when there are no actionable calls.

## Browser evidence

Native Chrome control exercised the built app against the isolated comparison database on port 3022:

- Mining case: initial selected 1911 Gold claim, evidence heading and source link all open at 12:09; selecting Banyan changes heading/source to 5:48, with its second span at 7:41.
- AI compute case: CRDO, META and NVDA listing matches display with issuer links and an explicit source-ticker distinction. Source evidence and existing Save/Inspect actions remain present.
- Visual screenshots inspected at desktop and Chrome responsive 390 × 844: the Finradar theme remains intact; notes wrap inside cards; mobile navigation and stacked cards remain usable.

## Live results

**All seven stopped runs completed.** With the previously completed cases, all 20 cohort videos now have completed analyses. This is operational completion, not 20/20 verified accuracy. Four focused quality regressions also completed using retained captions. The original baseline files, reference captures and run records remain unchanged.

| Case | Video | Previous failure | Accepted calls / context | Recovery USD |
|---|---|---|---:|---:|
| 1 | [SPIRV9UjNYU](https://www.youtube.com/watch?v=SPIRV9UjNYU) | Speech gaps | 1 / 7 | $0.0880 |
| 2 | [J_VpfkM74Wk](https://www.youtube.com/watch?v=J_VpfkM74Wk) | Invalid ASR timestamps | 0 / 7 | $0.1435 |
| 3 | [9nb3fp76Rz0](https://www.youtube.com/watch?v=9nb3fp76Rz0) | Speech-occupancy threshold | 0 / 7 | $0.1089 |
| 7 | [U32FPvvBaNI](https://www.youtube.com/watch?v=U32FPvvBaNI) | Evidence span over two minutes | 8 / 7 | $0.1467 |
| 11 | [ZfOQoh82JTo](https://www.youtube.com/watch?v=ZfOQoh82JTo) | 65:20 podcast; invalid ASR timestamps | 5 / 19 | $1.0485 |
| 12 | [iBMZc7zs_Ew](https://www.youtube.com/watch?v=iBMZc7zs_Ew) | 51:47 podcast; output truncation | 11 / 16 | $0.3342 |
| 20 | [Dy_0RtmAt1U](https://www.youtube.com/watch?v=Dy_0RtmAt1U) | Evidence span over two minutes | 2 / 5 | $0.0936 |

### Long-video audit

The 65:20 podcast completed with five accepted calls and 19 accepted context points. Four prior valid audio windows were reused. The final transcript contains 16 valid bounded windows and seven gap checks. Two additional timestamp failures were recovered by subdivision to 75-second clips. A final coverage audit found a 40-second interval left by a partly filled gap; another check recovered 15 cues before the final analysis was refreshed.

The final source has **no uncovered interval longer than three seconds**. Timed speech occupies 86.13% of the video; pauses are not represented as invented speech, so the original speech-coverage metric remains visible. Audio accuracy and anchor accuracy remain model-derived and unverified by an independent human/source. Eight extraction batches completed; no full-video transcription request was made.

The 51:47 captioned podcast completed in seven extraction batches with 11 calls and 16 context points. Its Banco Inter research remains visible as context even where the actionable-call policy differs from LeapEdge.

### Focused output-quality checks

- **Case 10, optical companies:** zero accepted calls in the baseline → two accepted calls. Coherent and the caption spelling “Lumenum” resolve separately to COHR and LITE with issuer links. Original evidence is unchanged. The unsupported LITE ticker proposal is retained separately; its company thesis is independently accepted. The final recheck replayed the three existing extraction responses and billed only the changed critique.
- **Case 16, company/sector groups:** nine accepted calls and eight context points. ServiceNow has a sourced NOW match; generic energy, insurance and cybersecurity groups remain named groups rather than silently becoming ETFs. This is not a claim of complete automatic entity coverage.
- **Case 18, options:** the relevant context preserves strikes 175 and 165 as strikes, support 155 separately, and the November 20 expiry in the explanation. Educational examples remain context rather than manufactured trades. Two explicit creator strategy calls are accepted.
- **Case 19, mining:** New Pacific’s 20% target is retained as a structured target. Six calls and six context points are accepted; a change in count alone is not a quality gain or loss.

### Comparison with retained LeapEdge outputs

No LeapEdge page was submitted for analysis again. The same captured outputs were reused:

- Case 1 agrees on the conditional long-term AI thesis; conviction differs.
- Case 2 produces no actionable calls in either system.
- Case 3 keeps Dell/semiconductor commentary as context, while LeapEdge labels three instrument stances. The text critic rejected overstated conviction in proposed calls.
- Case 7 covers the AMD/Wynn/RH/American Express themes, with different hold/conditional versus long classifications and additional explicitly described purchases.
- Case 11 has no usable LeapEdge reference: the captured LeapEdge run failed on length. Local completion does not create a comparison result.
- Case 12 retains Inter’s growth, lending protection and valuation scenario as context; LeapEdge packages that as one INTR long call. The local output also captures other explicitly discussed positions.
- Case 20 preserves the silver breakdown scenario and 54–60 area in context while retaining the creator’s holding instruction. LeapEdge labels the scenario “avoid.”

These are policy/interpretation differences, not independently established errors. No human-verified dataset or accuracy badge was created.

## Cost reconciliation

- Seven stopped-run recoveries: **$1.96350045**.
- Four focused quality checks, including the identity recheck: **$0.79067025**.
- Total additional model usage: **$2.75417070**, across 96 settled paid calls; zero open reservations.
- Native Google portion: **$1.12767070**, estimated from reported tokens and the versioned price table.
- OpenRouter/Claude portion: **$1.62650000**, provider-reported usage cost.
- Original 20-case API usage at corrected rates: **$2.17620600**. Combined API usage: **$4.93037670**.
- New TranscriptAPI credits: **0**. New LeapEdge analyses/credits: **0**.

These are model-usage amounts, not an invoice or a fresh-run price forecast. They include failed attempts and engineering rechecks; retained audio windows/transcripts were reused. The immutable original ledger remains $0.02555395 below the corrected-rate baseline because of the old Flash Lite table; historical rows were not rewritten. LeapEdge’s earlier displayed costs/credits are separate from these API totals.

## Verification

Both final full suites passed: 418 tests, 417 passed, one existing skip, zero failures in each backend mode. Type checking and the production build passed. The legacy promotion diagnostic remains advisory-only; it is not an accuracy gate.

Native Chrome desktop/mobile visual inspection and UI interaction checks passed: initial evidence/player timestamps agree, company ticker search finds sourced matches, accepted context expands with original evidence and translation, options strikes/support/expiry remain distinct, and the long-video result exposes five calls and 19 context points. Today reports zero unresolved runs and preserves all seven original failures as Recovered. The API regression verifies that only validated recovery IDs are exposed, with private inputs still omitted.

Recovery results remain in the isolated comparison database (local viewer on port 3022); this verification did not copy research into the production database. [PR #8](https://github.com/JoshuAI-888/leapedge-study/pull/8) merged as `b27c903` after both GitHub verify jobs and the Vercel preview passed. The Mac runtime was updated with the verified code/build; its web endpoint returned HTTP 200 and its worker heartbeat is fresh. Its active database and paused queue were preserved. Hosted preview access rendered correctly and its sampled runtime error/fatal logs were empty; full data/UI acceptance used the isolated local comparison database.

[Machine-readable results](recovery-20260920.json) contain per-video output summaries and local run IDs. Private raw evidence and call ledgers remain under ignored `data/recovery-final-20260920/`.

### Hosted follow-up

The merged production deployment reached READY. Browser smoke testing then found the production origin unset; `YTI_APP_ORIGIN` was set to the canonical production URL and redeployed. The access screen now renders, but `/api/access` returns 503 (hosted access is not configured), indicating the access secret is absent or too short. Automatic approval review rejected initializing a new production credential without explicit user authorization. That approval is pending; authenticated production data/UI verification is therefore incomplete. The local recovery results and Mac runtime verification above are unaffected.

Continue the standalone YouTube Intelligence build loop.

Read AGENTS.md, docs/delivery/standalone-scope.json, docs/delivery/standalone-build-loop.md and the delivery ledger. Run `npm run delivery:next`. Implement the next eligible Phase 2 feature, then Phase 3, following the build-feature skill and the current scope override. Use independent lanes only with disjoint ownership, as the build plan describes. Keep going through fixes and verification; stop before Phase 4.

Do not ask for fifty human-verified cases: the user removed them. Do not report legacy advisory diagnostics as quality approval. Add real Postgres concurrency/restart evidence and browser-control workflow/visual evidence before closing phase gates. Respect the existing US$25 total API / twenty LeapEdge analysis cap. Build before comparator runs. The local Mac is the approved worker host.

Keep a durable checkpoint in the delivery ledger and feature commits. Update the draft PR and report what is implemented, tested, browser-verified, live-verified, and still blocked. Never mark a feature merged while it only exists locally or on a branch. Do not stop because an old skill asks for human gold data or repeated intermediate implementation approval; the current scope overrides those instructions. Do stop dependent paid work if credentials or the spending cap prevent it, while continuing useful independent work.
